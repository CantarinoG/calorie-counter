import 'package:flutter/material.dart';

class MealFormScreen extends StatelessWidget {
  const MealFormScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Text("Meal Form Screen");
  }
}
